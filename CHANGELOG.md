# Changelog

Recipe Archive의 변경 사항을 기록합니다.

## v1.6 — 2026-09-05

### 추가
- 사진 자르기(크롭)
  - 오른쪽 패널 상단의 '자르기' 탭으로 전환하면, 미리보기 위에서 직접 영역을 지정합니다.
  - 자르기 화면에서는 항상 원본 전체가 보이고 그 위에 선택 영역만 표시됩니다.
  - 영역 이동, 네 모서리와 각 변 중앙 핸들로 크기 조절, 삼분할 가이드선 제공
  - 자르기 비율: 자유 / 1:1 / 4:5 / 5:4 / 3:4 / 4:3 / 3:2 / 2:3 / 9:16 / 16:9
  - 마우스와 터치 모두 지원
- 사진 기울기 조정 (수평 맞추기)
  - 슬라이더로 -15°~+15° 조정, 1° 단위 버튼과 0° 초기화 제공
  - 회전으로 생기는 빈 모서리는 자동으로 잘라내어 여백 없이 채워집니다
- 오버레이 중앙 정렬 옵션
  - 촬영 정보는 중앙 위, 필름 레시피는 중앙 아래에 배치됩니다.
  - 좌우 배치와 같은 형식으로, 항목별 줄바꿈과 필름 시뮬레이션 시그니처 색상의 구분선이 적용됩니다.
  - 사진과 글자를 구분하는 방식을 '배경 어둡게'와 '글자 윤곽선' 중에서 고를 수 있습니다.
- 출력 비율에 '원본'과 3:2 / 2:3 추가
- 홈 화면에 추가하면 앱처럼 전체화면으로 실행됩니다 (PWA)

### 모바일
- 미리보기를 누르거나 사진을 끌어다 놓아도 업로드할 수 있습니다 — 사진을 올리려고 화면 아래까지 내려갈 필요가 없어졌습니다
- 미리보기가 화면 맨 위에 오도록 순서 변경
- 모든 버튼·체크박스·핸들의 터치 영역을 44px 이상으로 확대
- 내보내기 버튼을 화면 하단에 고정
- 자르기 탭을 누르면 미리보기 위치로 자동 스크롤
- 입력 시 화면이 확대되던 문제, 주소창 높이로 화면이 어긋나던 문제 수정
- 아이폰 노치·홈 인디케이터 영역을 피해 배치
- 가로 모드 전용 레이아웃 추가

### 수정
- 자르기 중 마지막 사진을 삭제하면 빈 화면 위에 자르기 영역이 남아 있던 문제
- EXIF가 없거나 손상된 파일을 불러올 때 콘솔에 오류가 기록되던 문제
- 오버레이 배치에서도 출력 비율을 선택할 수 있도록 변경 (이전에는 사진 비율로 고정)
- 오버레이 중앙 정렬의 세로 정렬이 일부 비율에서 어긋나던 문제 (불균형 1.21배 → 1.01배)
- 카메라 이름과 필름 시뮬레이션 이름의 글자 크기가 달라 이질감이 있던 문제
- 오버레이 '글자 윤곽선'의 선이 너무 굵어 글자가 뭉툭해 보이던 문제 (16% → 6%)
- 오버레이 표시 방식 선택 버튼이 화면에 나타나지 않던 문제
- 자르기 비율을 고르면 출력 비율이 4:5로 되돌아가던 문제
- 사진을 불러오기 전에 화면 중앙에 십자 모양이 보이던 문제

### 내부 정리
- 렌더링 함수 분리 — renderToCanvas 360줄 → 199줄
- 중복된 자르기 영역 재배치 로직 통합
- 리팩터링 전후 9,000개 조합의 그리기 명령을 비교해 출력이 완전히 동일함을 확인

## v1.5 — 2026-09-04

### 추가
- 촬영 정보에 사용 렌즈 표시 (@nomad_anywhere_)
  - EXIF LensModel에서 렌즈명을 읽습니다. 렌즈명이 없는 사진은 초점거리(예: `23mm`)로 대체 표시합니다.
  - 프레임 표시 여부를 켜고 끄는 옵션과, 직접 수정할 수 있는 입력란을 제공합니다.

### 수정
- 필름 레시피가 두 줄로 나뉠 때 줄마다 글자 크기가 달라지던 문제 — 한 문장에서 나뉜 줄은 항상 같은 크기로 표시됩니다.
- 줄바꿈 시 마지막 줄만 짧아지던 문제 — 각 줄의 길이가 비슷하게 배분됩니다. (폭 불균형 4.1배 → 1.4배)
- 후지 렌즈명 뒤에 붙던 불필요한 널 문자 제거

### 성능
- 텍스트 폭 측정 결과 캐싱 — 프레임 1회 렌더의 폰트 설정 호출 144회 → 48회
- 입력 중 렌더링 합치기 — 글자를 칠 때마다 전체를 다시 그리던 것을 화면 갱신 주기 단위로 합쳐, 15자 입력 시 15회 → 1회 렌더
- 사용하지 않는 웹폰트 굵기 제거 (JetBrains Mono 500, M PLUS 1 Code 500, Nanum Gothic Coding 700)

### 검토 중
- 사진 하단에만 정보를 배치하는 전용 프레임 레이아웃 (@offetuoso_) — 현재 미지원, 추가 여부 검토 중

## v1.4 — 2026-09-01

### 추가
- 필름 레시피 화이트밸런스에 켈빈값(K) 표시 (@hayasaka_jp)
  - 표시 형태: `Kelvin 5500K R+2 B-4`
  - 색온도로 직접 설정한 경우에만 기록되며, 프리셋 촬영 시에는 나타나지 않습니다.

## v1.3 — 2026-09-01

### 추가
- 출력 파일 형식 선택: PNG / JPEG / WEBP (@ilce7m3)
  - HEIC는 브라우저에 인코더가 없어 지원 불가.
- 촬영 정보와 필름 레시피에 화이트밸런스 각각 표시

### 수정
- Android에서 사진 앱에 저장되지 않던 문제 (@o_pyris)
  - iOS는 공유 시트, Android·PC는 다운로드 방식으로 분리.
- 2장 이상 내보낼 때 한 장씩 저장해야 하던 문제 (@leon_jeon8864)
  - PC·Android에서 ZIP 하나로 저장.

## v1.2 — 2026-08-31

### 추가
- iOS 공유 시트를 통한 저장
- 2장 이상 ZIP 저장 (외부 라이브러리 없이 구현)

### 수정
- 레시피가 위/아래/오버레이 배치에서 잘리던 문제 — 전체 항목 표시, 길면 자동 줄바꿈
- '사진 위' → '오버레이'로 명칭 변경
- 사진 안 위치 설정은 오버레이 선택 시에만 표시

## v1.1 — 2026-08-30

### 추가
- 다국어 지원 (한국어 / English / 日本語, 시스템 언어 자동 감지)
- 4K(3840px) 출력
- 개인정보처리방침

### 수정
- 한글·일본어가 가변폭 폰트로 표시되던 문제 (모노스페이스 웹폰트 추가)
- 저사양 기기에서 4K 내보내기 시 메모리 초과 방지

## v1.0 — 2026-08-28

### 최초 배포
- EXIF 촬영 정보(제조사·모델·조리개·셔터스피드·ISO·날짜)를 프레임에 표시
- 후지필름 필름 시뮬레이션 레시피 자동 인식
- 출력 비율 7종, 정보 배치 5종(위/아래/좌/우/오버레이), White/Black 배경
- 모든 처리는 브라우저 내에서만 수행
